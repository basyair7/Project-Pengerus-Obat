#ifndef LCDDISPLAY_HPP
#define LCDDISPLAY_HPP

#include <Arduino.h>
#include <LiquidCrystal_I2C.h>
// #include <LiquidCrystal_animated.h>
#include "variable.h"

class LCDdisplay : protected VariableMillis {
public:
    void init();
    void clear();
    void print(const String& x, const uint8_t col, const uint8_t row);
    // void printAnimated(const String& x, const uint8_t col, const uint8_t row);

private:
    LiquidCrystal_I2C* lcd = new LiquidCrystal_I2C(0x27, 16, 2);
    // LiquidCrystal_animated* animated = new LiquidCrystal_animated;

};

#endif